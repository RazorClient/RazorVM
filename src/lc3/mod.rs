pub mod cpu; // CPU-related functionality (instruction execution, decoding)
pub mod hardware; // Hardware-related functionality (memory, registers, flags)
pub mod opcode; // Opcode definitions and mappings
pub mod vm;

